<form id="yonny-foto-form" class="ui inverted form" method="POST" action="/admin/yonny-fotos/">
  <input type="hidden" name="id" value="{{yonnyFoto.id}}">
  <div id="errorMsgBox" class="ui error message">

  </div>
  <div class="inline field">
    <div class="ui toggle checkbox">
      <input type="checkbox" tabindex="0" name="visible" class="hidden" {{#if yonnyFoto.visible}}checked{{/if}}>
      <label>Visible</label>
    </div>
  </div>
  <div class="field">
    <label>Title</label>
    <div class="ui underlined input">
      <input placeholder="Titel" type="text" name="title" value="{{yonnyFoto.title}}" required>
    </div>
  </div>
  <button class="ui button" type="submit">Erstellen</button>
</form>
